# 回测

## 适用场景

回测能力用于从 DojoSDK 或本地数据源获取历史数据，运行本地或远程回测任务，并将结果返回给 Dashboard 展示。

## 当前设计方向

- 数据从 DojoSDK 或本地 store 获取。
- 回测框架适配本地和线上两类执行能力。
- 本地回测结果需要持久化，方便前端查询。
- 远程回测在任务结束后通过相关接口读取结果。
- 可视化重点是净值曲线、收益回撤和关键指标。

## 状态

当前页面是功能入口；详细实现需要在后续迭代中继续补齐到本页。
